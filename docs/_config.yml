title: Linked Data Interactions 1
description: A starter template for a Jeykll site using the Just the Docs theme!
remote_theme: just-the-docs/just-the-docs@v0.5.2

url: https://just-the-docs.github.io

favicon_ico: "/assets/images/favicon.ico"

aux_links:
  LDI Repository: https://github.com/Informatievlaanderen/VSDS-Linked-Data-Interactions

# Define Jekyll collections
collections:
  core:
    permalink: "/:collection/:path"
    output: true
  ldio:
    permalink: "/:collection/:path"
    output: true
  ldi-nifi:
    permalink: "/:collection/:path"
    output: true

just_the_docs:
  collections:
    core:
      name: Linked Data Interactions Core Building Blocks
    ldio:
      name: Linked Data Interactions Orchestrator
    ldi-nifi:
      name: Linked Data Interactions for Apache NiFi

mermaid:
  # Version of mermaid library
  # Pick an available version from https://cdn.jsdelivr.net/npm/mermaid/
  version: "9.1.3"